# PyTorch: the default `pip install torch` wheel is often CPU-only. For NVIDIA GPU acceleration,
# reinstall PyTorch with CUDA from https://pytorch.org/get-started/locally/ (same venv). Example:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# Verify: python -c "import torch; print(torch.cuda.is_available(), torch.version.cuda)"
# WhisperX pins torch~=2.8 in metadata; newer CUDA stacks (e.g. torch 2.11+cu128) often work but
# pip may report conflicts — prefer matching WhisperX versions when downloads succeed.

PySide6>=6.6.0,<7
python-dotenv>=1.0.0,<2
whisperx>=3.1.0,<4
torch>=2.0,<3
huggingface_hub>=0.26.0,<2
httpx>=0.27.0
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.21.1.3
nvidia-cuda-runtime-cu12==12.9.79
