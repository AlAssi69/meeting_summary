# CUDA PyTorch wheels for GPU inference image (installed after requirements-container.txt).
# Base image: nvidia/cuda:12.9.0-cudnn-runtime-ubuntu24.04 — PyTorch may bundle cu12 12.8.x pip wheels; that is expected.
# Do not pin nvidia-* packages here; let torch resolve compatible nvidia-cublas/cudnn/cuda-runtime versions.
--extra-index-url https://download.pytorch.org/whl/cu129
torch>=2.8,<2.9
torchaudio>=2.8,<2.9
torchvision>=0.23,<0.24
